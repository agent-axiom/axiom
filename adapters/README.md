# Adapters

This directory is reserved for model and host-agent adapters.

The bootstrap slice keeps execution deterministic and local, but the runtime boundaries are already shaped so future adapters can expose:
- direct model backends
- local model servers
- external coding-agent hosts
