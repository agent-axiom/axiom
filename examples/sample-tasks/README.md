# Sample Tasks

Populate this directory with representative AXIOM task files as the workflow stabilizes.

The bootstrap implementation exercises task creation dynamically under `.axiom/tasks/`, so no checked-in sample tasks are required yet.
